# 深柴能源主网站交接文档

更新日期：2026-08-01

## 当前状态

主站源码、双语文案和生产构建已同步。常规启动、构建、目录说明以 [README.md](README.md) 为准；本文件记录本轮设计决策、已验证状态和继续维护时容易踩坑的部分。

当前没有已知阻塞问题。2026-08-01 已继续完成桌面媒体恢复、产品展厅短视窗排版和“探索产品”悬停播放逻辑修复；本轮重新执行 `pnpm run lint`、`pnpm run check:videos` 与 `pnpm run build` 均通过。此前 360×800、390×844、412×915 手机端与 1440×900 桌面端共 7 个入口的统一站点回归结果仍然有效。

### 最近同步核验（2026-07-23）

- 当前 `dist/` 已由清理后的源码重新生成，生产构建包含最新首页开屏和全部页面逻辑。
- 已确认 `src/` 与生产 JS 均不含 `SpotlightHome`、`toggleSpotlight`、`showSpotlight` 或 `generator-reveal.jpg` 请求；`src/components/SpotlightHome.jsx` 已删除。
- 当前首页仅保留一个主视频开屏板块和“探索产品”入口；无左右切换或聚光灯互动页。
- 本次核验再次执行 `pnpm run check:videos`，结果为 6/6 通过。

### 代码与部署瘦身（2026-07-23）

- 删除未再使用的 `App.css`、旧首页组件 `Home2.jsx`、`Home3.jsx` 和聚光灯组件，清除相应旧样式与 58 个失效双语文案键。
- 清理 434 条仅服务于旧页面的 CSS 选择器；生产 CSS 由约 107.82 KB 降至约 74.92 KB。
- `public/` 和 `src/assets/` 已按源码引用逐项核对，当前没有未引用的部署资源。
- 本地快照、原始素材、旧 3D 导出、备份图片和未引用模型已移出项目，共归档约 402.48 MB，不再进入 GitHub 或 Cloudflare 构建。
- `dist/` 由约 114.78 MB 降至约 62.58 MB，最大单文件约 21.87 MB，没有文件超过 Cloudflare Pages 的 25 MiB 单文件限制。
- Cloudflare Pages 使用 `pnpm run build`，输出目录为 `dist`；项目只保留 `pnpm-lock.yaml`。
- GitHub Desktop 的 100 MB 报错来自 `.codex-snapshots/before-global-color-harmony-20260723-142033.zip`（205.21 MB）。该文件已从唯一一条尚未推送的本地提交中移除，并通过 amend 重建提交；待推送对象中没有 ZIP、快照目录或超过 100 MB 的文件。
- GitHub 仓库副本位于 `C:\Users\29830\Documents\GitHub\深柴1.2`，当前工作区干净、相对 `origin/main` 领先 1 条提交；尚未由本轮自动推送，可直接在 GitHub Desktop 中再次 Push。
- 清理前恢复包：`E:\深柴网站总文件\部署文件\深柴动力主页1.0-清理归档\before-slim-20260723-204709.zip`。
- 恢复包 SHA256：`1741E503C72C50FCBEDA38B0982AAF410AFB37C49DBF13BC3F69B061EA4FDFA6`。

## 本轮最终页面结构

开屏视频之后立即进入重建的“为什么选择我们 / 资质矩阵”模块，随后才是产品中心：

1. `01 / PROOF OF RELIABILITY` — 为什么选择我们 / 资质矩阵
2. `02 / PRODUCT SYSTEMS` — 产品中心
3. `03 / APPLICATION FIELDS` — 聚焦行业
4. `04 / COMPANY PROFILE` — 关于我们
5. `05 / PROJECT CASES` — 项目案例
6. `06 / SALES & SERVICE` — 销售与服务

首页顺序由 `src/App.jsx` 控制。资质矩阵标题中的 `/ 06` 表示 6 项证书数量，不是页面模块编号，不要随页面顺序改动。

## 今日同步的滚动覆盖转场

- “交付能力，也有资质佐证。”到“探索产品”：`src/components/Advantages.jsx` 新增 `.advantage-certifications-stage`；资质内容在转场阶段保持，浅色产品区通过 `src/main-redesign.css` 的负外边距、层级和圆角面板从底部向上覆盖。使用真实滚动位置推进，上滑自动回退，不使用渐变淡入或延迟动画。
- “关于深柴能源”到“项目案例”：`src/components/About.jsx` 新增 `.about-cover-stage` 与 `.about-cover-panel`，固定关于内容和数据卡片作为被覆盖层；`src/main-redesign.css` 为 `.cases-section` 增加上移覆盖、层级、顶部圆角和实色阴影。
- 项目案例使用真实案例素材，保留中轴曲线、随滚轮成组插入的左右卡片和上滑回退逻辑；对应组件为 `src/components/Cases.jsx`，素材位于 `public/case-archive/`。
- 销售与服务已重构为联系方式优先的服务控制台：取消在线留言表单，电话和邮箱同等突出、上下连通且无分割线，同时保留地址、工作时间、服务范围和公司据点信息；对应组件为 `src/components/Contact.jsx`。
- “为什么选择我们”背景按参考图改为 `#111F2D` 深灰海军蓝，保留低透明度工业网格；该色值只作用于该模块，不改变探索产品的浅色基调。
- 两处转场共用“面板保持 + 下一模块上移覆盖”的节奏。桌面端已在 Chrome 本地页面验证起点、覆盖完成点及上滑回退；中文与英文标题均正常。
- 相关 CSS 变量位于 `src/main-redesign.css` 顶部：`--qualification-cover-depth`、`--qualification-cover-travel`、`--about-cover-depth`、`--about-cover-travel`。调整页面间距时要同步检查覆盖起点，避免出现空白带或过早遮挡。

## “为什么选择我们”模块

- 组件：`src/components/Advantages.jsx`
- 样式：`src/main-redesign.css` 中 `Why ShenChai: proof-led archive` 区块
- 文案：`src/i18n.jsx` 中 `adv*` 键
- 锚点：`#advantages`
- 4 组理由：制造、整机验证、复杂项目交付、现场工程能力
- 10 张真实现场图：`public/why-*.webp`
- 6 张资质证书：`public/why-certificate-*.webp`

已解决的问题：

- “现场工程能力”含横竖比例不同的素材，现使用固定画框和定向 `object-position` 裁剪，不替换原图。
- 桌面端模块外框固定 `640px`，图片区约 `638px`；切换 4 个理由时外框、档案区位置和整页高度不再变化。
- 右侧理由栏使用 68px 标题行与 4 个等高内容行；英文长标题和说明不会再裁掉第 4 项。
- 父容器使用不可滚动裁切，避免点击最后一项时内部自动滚动并在底部出现空白。
- 手机端图片区约 `515px`，4 项英文与中文均完整，无横向页面溢出。

## 开屏视频

- 当前文件：`public/hero-power-range-sharp4k.mp4`
- 播放结束后停在最后一帧，不自动进入下一模块。
- 离开首页区域再返回时从头重新播放。
- 相关逻辑：`src/components/Hero.jsx`
- 历史视频备份已移至项目外部清理归档，不参与源码或部署。

## 首页开屏

- 首页开屏仅保留主视频与“探索产品”入口；已移除“可靠动力，由内而生”的聚光灯互动页面及左右切换入口。
- 组件：`src/components/Hero.jsx`。

## 其他已确认功能

- 顶部导航和 Logo 正常，中英文切换正常。
- 产品中心保留原底色、圆角、左右进入效果、视频框尺寸与悬停播放。
- 行业模块为持续旋转图片环，无左右切换按钮，悬停不会暂停。
- 主站与各子页面 Logo 统一使用 `public/logo.svg`。
- “关于我们”使用 `public/flydeer-building.png`，外部子站链接仍为 `https://3-0-1.pages.dev/`。
- 产品展厅外部链接仍为 `https://shenchai1-5-3.pages.dev/`。

## 2026-07-24 顶部导航统一

- `src/components/Header.jsx` 已改为全站标准导航，并根据滚动位置高亮首页、产品中心、关于我们、项目案例和销售与服务。
- `src/index.css` 固定桌面端 `70px` 导航高度、`36px` Logo 和深蓝底色；手机端为 `64px` 导航高度、`28px` Logo。
- “获取报价”按钮使用固定 `6px` 圆角和滚动合成保护，已实测在顶部与下滑 `1100px` 后保持一致。
- 中英文栏目文案及跳转目标已与所有子站统一。
- 2026-07-24 已重新执行生产构建，并通过 `1440×900`、`390×844` 浏览器检查。

## 2026-07-24 全站联系方式统一

- 主站销售与服务区域、页脚以及右下角三个悬浮圆形入口已统一使用电话 `+86 182 0593 8836` 和邮箱 `flydeerpower@googlel.com`。
- 电话拨号链接统一为 `tel:+8618205938836`；WhatsApp 入口统一为 `https://wa.me/8618205938836`；邮件链接统一为 `mailto:flydeerpower@googlel.com`。
- 5 个产品子站和“关于我们”子站没有独立写死的电话或邮箱，联系入口均跳转主站，因此由主站统一承接，不再保留旧联系方式。
- 修改后已重新执行生产构建；当前活动站点源码和 `dist/` 中未检出旧的 `400-888-6666`、`info@shenchai.com` 或旧 WhatsApp 链接。

## 2026-07-24 跨站语言保持

- 新增 `src/languageRouting.js`：优先读取网址中的 `?lang=zh` 或 `?lang=en`，其次读取当前站点的本地语言记录。
- 切换语言会同步更新当前网址、HTML `lang` 属性和本地记录；主站导航、产品详情及“关于我们”入口会继续携带语言参数。
- 该方案不依赖跨域 Cookie，适用于当前多个独立 Cloudflare Pages 域名。进入其他站点后会自动保持中文或英文。
- 主站语言切换已使用可键盘操作的按钮，并通过 `aria-pressed` 暴露当前状态；视觉样式保持不变。
- 已通过真实浏览器验证主站 → 产品站 → 关于我们站的中英文双向传递，并重新构建 `dist/`。

## 素材与备份

- 线上优势图片和证书均为 WebP，放在 `public/`，并已确认被当前源码引用。
- 原始 JPG/PNG、历史视频、旧 3D 导出和未使用模型已移到 `E:\深柴网站总文件\部署文件\深柴动力主页1.0-清理归档\`，不再放在项目根目录。
- 需要重新裁剪或压缩时应从外部归档恢复原始素材，不要把归档目录或 ZIP 再提交到 Git。
- `.gitignore` 已忽略本地快照、历史素材目录、压缩包、`node_modules/` 和 `dist/`。

## 继续维护时的检查重点

- 调整 `Advantages.jsx` 文案时，同时检查英文卡片是否仍能在 4 个等高行内完整显示。
- 不要把 `.advantage-proof-console` 改回可滚动的 `overflow: hidden` 容器；这会重新触发内部自动滚动问题。
- 修改首页模块顺序时同步更新 01–06 编号，并确认资质数量 `/ 06` 未被误改。
- 修改导航目标地址时检查 `src/components/Header.jsx`。
- 发布前运行 `pnpm run check:videos` 和 `pnpm run build`。
- 桌面端与 390px 手机端都需要检查中英文版本。
- `scripts/check-video-assets.mjs` 已改为检查当前首页视频 `hero-power-range-sharp4k.mp4`；不要改回已不存在的 `hero-video.mp4`。

## 建议技能

- `frontend-design`：涉及页面重排、视觉调整和双语排版时使用。
- `browser:control-in-app-browser` / `chrome:control-chrome`：修改后检查真实布局、切换交互和响应式尺寸。
- `diagnosing-bugs`：再次出现高度跳动、视频失效或浏览器差异时使用。
- `handoff`：下一轮结束前继续同步本交接文件和临时会话摘要。

## 2026-07-24 英文品牌名与标题排版

- 英文品牌名已统一为 `FLYDEER POWER`，覆盖浏览器标题、首页开屏、关于我们、优势模块、页脚、Logo 辅助文本与图片说明。
- `src/index.css` 负责首页开屏品牌标题的字距、行高和响应式字号；`src/main-redesign.css` 末尾的 `Industrial title system` 负责各模块大标题、销售与服务标题和中英文移动端排版。
- 英文模块标题使用更宽的可用行宽和较克制的负字距；中文标题使用略松的行高，避免中文笔画相互压迫。
- 已重新生成 `dist/`，并扫描确认生产产物无旧英文品牌名残留。

## 2026-07-29 单站合并与一键部署

- 主站与 5 个产品展厅、关于我们页面已合并为同一个 Vite 多页面应用（MPA），统一从 `深柴动力主页1.0` 构建一个 `dist/`。
- 统一入口如下：
  - `/`：主站首页
  - `/about/`：关于我们
  - `/products/open-frame-500/`：500kW 开放式机组（原 1.3）
  - `/products/open-frame-1200/`：1200kW 开放式机组（原 1.6）
  - `/products/silent/`：静音型机组（原 1.7）
  - `/products/mobile-trailer/`：移动拖车型机组（原 1.8）
  - `/products/high-voltage/`：高压配电系统（原 1.9）
- 产品页面源码分别位于 `products/<slug>/`，产品模型与图片位于 `public/product-assets/<slug>/`，共享 Draco 解码器与询价背景位于 `public/product-assets/shared/`。这样既保持每个 3D 页面生命周期隔离，也避免再通过外部 `pages.dev` 域名跳转。
- 关于我们原静态页面已放入 `public/about/`，构建后会原样输出到 `dist/about/`；页面内资源和导航均已改为同源路径。
- 同源语言状态统一使用 `flydeer-language` 本地存储，主站、产品页、关于我们之间切换中文/English 后可以直接继承；内部链接同时保留 `?lang=zh|en` 兼容。
- 进入任一产品展厅后，顶部导航会高亮“网上展厅”（英文为 `Online Showroom`），不再误高亮“产品中心”。
- 旧的 `深柴1.3`、`深柴1.6`、`深柴1.7`、`深柴1.8`、`深柴1.9`、`深柴3.0` 目录暂时保留为恢复源，不参与统一构建，也不要把它们复制进 GitHub 仓库。
- Cloudflare Pages 构建配置：构建命令 `pnpm build`，输出目录 `dist`，安装依赖使用仓库内 `pnpm-lock.yaml`。当前统一产物没有单个文件超过 100 MB，且所有页面入口均已生成。
- 发布前建议执行：

  ```text
  pnpm install --frozen-lockfile
  pnpm build
  ```

  然后将 `dist/` 作为 Cloudflare Pages 的部署目录。不要上传 `node_modules/`、历史归档、快照 ZIP 或旧子站目录。

## 2026-07-31 媒体与手机端最终交接

### 已完成

- 主站手机端开屏使用完整 16:9 视频画框；标题、说明、CTA 与后续模块按正常阅读流排列，不再放大裁切视频，也没有首屏后的空白占位。
- 行业模块在手机端采用横向滚动的完整图片卡片；项目案例和服务卡片减弱暗色遮罩，保证图片细节和文字均可辨认。
- 关于我们页面在 700px 以下取消桌面端粘性舞台、负外边距和透视位移，改为项目、公司介绍、工厂、交付、产品和服务的正常阅读流；图片采用固定比例横向卡片。
- 5 个 3D 产品页使用 `products/shared/product-mobile.css` 统一手机端展示：完整模型、独立圆角模型台、正常标题/说明面板、底部可横向滚动的 44px 以上阶段按钮。
- 产品相机根据屏幕宽高比自动调整距离和模型比例；首屏模型不再被放大截断。模型与内容在加载完成后立即可见，不依赖先滚动才能出现。
- 修复产品页根节点固定 `height:100%` 导致手机端文档仅一屏、阶段按钮无法推进的问题。当前 5 个产品页手机文档高度约 4,575px，点击阶段按钮会真实滚动并激活对应阶段。
- 高压页面已替换全部“开发中”占位文字和白色占位图，使用正式的高压配电系统中英文文案。
- 全部产品页品牌年份统一为 2002，第一阶段统一为“产品总览 / Product Overview”。
- 产品页与关于我们页面补齐站点图标，真实浏览器不再产生 favicon 404。

### 性能与部署

- 首页和关于我们分别提供手机端视频版本；图片主体转换为 WebP，非首屏图片使用懒加载和异步解码。
- `public/_headers` 为版本化静态资源、图片、视频和 3D 模型配置 Cloudflare 浏览器缓存。
- `scripts/check-video-assets.mjs` 当前检查 8 个 MP4 的存在性、大小和文件头。
- `pnpm run lint` 只检查可维护的 `src/` 和 `products/` 源码，第三方 Draco 解码器不再混入代码质量输出；本轮同时删除了 4 个未使用的旧灯光控制器、5 条失效路径常量和无效参数。
- 2026-07-31 生产构建：161 个文件，约 95.26 MiB；最大文件为约 6.39 MiB 的 GLB，0 个文件超过 25 MiB，0 个文件超过 100 MiB。

### 已执行回归

- 手机端：360×800、390×844、412×915。
- 桌面端：1440×900。
- 路由：主站、关于我们、静音型、高压、移动拖车、1200kW 开放式、500kW 开放式。
- 检查项：菜单打开/关闭/Escape、导航触控尺寸、中英文保持、页面横向溢出、图片和视频可见性、3D 展示台尺寸、阶段按钮点击与滚动、控制台与页面错误。
- 结果：全部通过。

### 维护注意

- 手机端主站样式集中在 `src/mobile-site.css`；产品手机端共享样式集中在 `products/shared/product-mobile.css`。不要再把产品页根节点恢复为固定 `height:100%`。
- 桌面 3D 交互保留原舞台逻辑；手机端使用相同模型和阶段数据，但采用独立展示台与正常纵向滚动。
- 修改视频后必须同时更新桌面和手机来源，并执行 `pnpm run check:videos`、`pnpm run build` 和真实手机尺寸浏览器回归。

## 2026-08-01 桌面媒体与产品展厅稳定性修复

### 本轮修复

- 新增 `src/components/BrandLogo.jsx` 作为主站与 5 个产品展厅共用的品牌 Logo 组件。Logo 首次请求失败时会自动重试，仍失败才显示文字后备；图片恢复后会完全隐藏后备文字，避免透明 PNG 下出现重影。
- `index.html` 预加载 `/logo.svg`；首页和产品页导航统一使用同一 Logo、同一加载策略与同一无障碍文本。
- 首页“探索产品”视频采用最终确认的交互：默认停在第一帧，鼠标进入时从第一帧播放，鼠标移出后暂停并回到第一帧，可重复触发；不再继续压缩该模块视频。
- 项目案例首屏优先显示的 4 张图片改为高优先级加载，并增加临时请求失败后的有限重试；图片成功解码后再显现，避免浏览器短暂显示破图图标。
- 产品页阶段按钮清除浏览器默认按钮背景与外观，解决桌面端出现白色胶囊条的问题。
- `products/shared/product-mobile.css` 增加桌面短视窗布局：阶段导航固定在独立底部控制轨内，标题、模型和说明在 960×616、1180×700、1435×616、1435×900 等尺寸下保持互不遮挡。

### 本轮验证

- `pnpm run lint`：通过。
- `pnpm run check:videos`：8/8 通过。
- `pnpm run build`：通过；`ProductNavigation` 大于 500 kB 仅为 Vite 构建提示，不影响部署。
- 5 个产品入口 × 5 个阶段 × 4 个桌面视口，共 100 组桌面布局检查通过。
- “探索产品”视频的默认暂停、悬停播放、移出复位和再次悬停播放均通过自动化检查。
- 快速滚动到项目案例时，前 4 张图片连续 4 轮均成功显示。
- Logo 和案例图片的首次请求故障恢复逻辑已加入回归检查；继续维护时应保留 `BrandLogo.jsx` 中的有限重试和后备切换逻辑。

### 同步与发布注意

- 权威源码仍为 `E:\深柴网站总文件\部署文件\深柴动力主页1.0`；GitHub 工作副本为 `C:\Users\29830\Documents\GitHub\深柴1.2`。
- 本轮只同步源码、配置和交接文档；`dist/`、本地测试截图、缓存与历史归档不进入 Git。
- Cloudflare Pages 继续使用构建命令 `pnpm run build`，输出目录 `dist`。部署后若浏览器仍显示旧 Logo 或旧交互，先核对部署提交，再执行强制刷新以排除浏览器/CDN 缓存。

## 2026-08-01 响应式交互补充修复

### 修复内容

- 5 个产品展厅的桌面端“产品切换”按钮改为在标题卡片下方占用独立位置；英文标题在 901–1180px 窄桌面窗口下使用额外垂直避让，避免按钮覆盖标题。
- 主站手机端产品详情恢复默认收起；只有点击“展开 / Expand”后才显示详情，再次点击可完整收起。
- 主站手机菜单移除旧版 `height:100%` 导航项影响，6 个主导航项恢复为紧凑的 50px 行高；导航、报价按钮与社交入口恢复深蓝背景上的高对比文字。

### 验证结果

- `pnpm run lint`：通过。
- `pnpm run check:videos`：8/8 通过。
- `pnpm run build`：通过；仅保留既有的 `ProductNavigation` 大代码块提示。
- 5 个产品页 × 4 个桌面视口 × 中英文，共 40 组产品切换按钮、标题和下拉菜单边界检查通过。
- 主站在 360×800、390×844、412×915 的中英文版本中，产品详情默认收起、展开、再次收起共 6 组检查通过。
- 同一组手机视口与语言下，菜单打开、6 个导航项可见、报价与社交文字对比度、Escape 关闭和页面滚动解锁共 6 组检查通过。

## 2026-08-01 网上展厅与关于页手机菜单修复

### 修复内容

- 5 个产品展厅的手机菜单项明确覆盖桌面端 `height:100%`，恢复为连续的 50px 行高；菜单打开后首页、产品中心、网上展厅、关于我们、项目案例、销售与服务均在首屏可见。
- 产品展厅手机页隐藏桌面端语言与社交控制区，语言切换保留在菜单操作区，避免顶部出现两套控制和挤压 Logo。
- 关于我们手机菜单同样恢复 50px 导航行高，并将导航内容固定从顶部开始排列，不再因桌面端垂直居中规则产生大段空白。

### 验证结果

- 原问题复现时，产品展厅和关于页的每个菜单项高度均约 741px；修复后均为 50px。
- 5 个产品页与关于页 × 360×800、390×844、412×915 × 中英文，共 36 组移动端菜单检查全部通过。
- 每组均验证：初始关闭、按钮打开、6 个栏目可见、操作区可见、产品页桌面控制区隐藏、Escape 关闭，以及控制台、页面和 HTTP 错误为 0。

## 2026-08-01 关于页首屏背景兜底修复

### 修复内容

- 线上 MP4 路径与编码均正常：手机视频为 H.264 High、`yuv420p`、1280×720、30fps。空白首屏的直接原因是页面没有视频封面或 CSS 背景图，微信/iOS 未启动自动播放时只能显示纯深蓝底色。
- 从手机视频的发动机画面生成 `public/about/home-hero-poster-v1.webp`，大小约 91KB；同时作为 `<video poster>` 与 `.hero` 背景图，即使 MP4 请求失败或视频无法自动播放也保持完整画面。
- 视频补充 `webkit-playsinline`、`x5-playsinline`，并在 `canplay`、`WeixinJSBridgeReady`、`pageshow`、页面重新可见与首次触摸时安全重试播放。

### 验证结果

- 微信 iPhone 用户代理下，正常视频资源可以播放且封面路径正确。
- 自动化阻断全部 MP4 请求后，91KB WebP 封面仍能加载并完整显示发动机背景，原纯深蓝空白问题不再出现。
- 5 个产品页与关于页的 36 组中英文手机菜单回归继续全部通过；`pnpm run lint`、8/8 视频资源检查与生产构建通过。

## 2026-08-01 关于页资质卡片触摸滑动修复

### 修复内容

- 资质卡片原本设置了 `touch-action: pan-y`，手指命中卡片后浏览器只允许纵向手势，导致外层虽为 `overflow-x:auto` 仍无法横向滑动。
- 将卡片恢复为浏览器原生手势处理，保留资质列表的横向滚动、滚动吸附以及页面纵向滚动。

### 验证结果

- 微信 iPhone 用户代理、390×844 视口下模拟真实触摸拖动，修复前资质容器 `scrollLeft` 为 0，修复后由 0 移动到 283px。
- 前后截图确认拖动后由第 1 张“质量管理”切换到第 2 张“环境管理”，卡片内容和后续页面布局正常。
- 5 个产品页与关于页的 36 组中英文手机菜单回归全部通过；`pnpm run lint`、8/8 视频资源检查与生产构建通过。

## 2026-08-01 全站手机顶部语言切换统一

### 修复内容

- 以“关于我们”手机顶部栏为唯一视觉基准，将主站和 5 个产品页的“中文 / English”切换移到菜单按钮左侧，菜单关闭时也始终可见。
- 删除主站与产品页展开导航里的重复语言按钮，避免同一页面出现两套切换入口。
- 新增共享 `src/mobile-header.css`，由主站和 5 个产品页共同引用；统一 64px 顶栏、98px Logo、12px 语言字号、6px 语言间距、44×44px 菜单按钮、10px 圆角、深蓝背景及红色激活下划线，桌面端布局保持不变。

### 验证结果

- 7 个页面 × 360×800、390×844、412×915，共 21 组手机顶部栏精确样式与交互检查全部通过。
- 每组均验证：菜单关闭时两个语言按钮可见、语言按钮不在导航内部、中英文切换有效、Logo/语言/菜单按钮互不重叠，以及菜单打开后顶部切换仍保持可见。
- 5 个产品页与关于页 × 3 个手机视口 × 中英文，共 36 组菜单回归全部通过；导航行高、操作区、Escape 关闭及控制台/页面/HTTP 错误检查均正常。

## 2026-08-01 品牌升级为深柴能源

### 变更内容

- 中文品牌名由“深柴动力”统一更新为“深柴能源”；英文品牌名 `FLYDEER POWER` 保持不变。
- 以客户提供的转曲生产文件为源，将新版 Logo 转为网站用矢量资源 `public/logo.svg`，主站、关于我们、5 个产品页及页脚共用同一份资源。
- 同步更新中文浏览器标题、页面文案、辅助文字和无障碍标签，移除活动源码对旧 PNG Logo 的引用。

### 验证要求

- 对主站、关于我们和 5 个产品页分别验证中英文版本：中文页面不得出现“深柴动力”，英文页面继续显示 `FLYDEER POWER`。
- 所有页面可见 Logo 必须加载 `/logo.svg`，并检查自然尺寸、显示状态、移动端顶部栏以及页面菜单交互。
- 首页中文开屏标题必须直接检查可见 H1 为单一实心“深柴能源”，不能只检查页面其他位置是否出现新品牌；英文 `FLYDEER POWER` 保持两行实心排版，禁止重新使用描边叠字效果。
- 首页 `public/flydeer-building-energy.webp` 与关于页 `public/about/company-profile-energy.webp` 中原先压入照片的楼体字样也已替换为“深柴能源”；两条路径使用同一份 `1833×858` WebP 成品，并通过新的文件名绕过旧图片 30 天浏览器缓存，避免两个页面继续显示旧品牌。
- “为什么选择我们”图片理由区的 4 个切换按钮必须始终保持 `opacity: 1`，不可再通过 DOM 临时添加 `visible` 类控制显示，否则 React 状态切换会覆盖类名并造成按钮消失。
